function [t, P] = igraca(o, do)% Resi diferencialno enacbo oz. doloci gibanje igrace    % VHODNI PODATKI : % o = [x; y], do = [dx; dy] (otrok)    % IZHODNI PODATKI:% P: polozaj igrace po casih t, tj. matrika z x in y koordinatami% t: vektor s časi    % v isti vrstici je v vektorju t podatek o času, v matriki P pa% položaj ob tem času t    % zapis diferencialne enacbe    odefun = @(t, P) ((norm(do(t)) / norm(o(t) - P)) .* (o(t) - P)) .* ((do(t)' * (o(t) - P)) / (norm(do(t)) * norm(o(t) - P)));     % resitev diferencialne enacbe    [t, P] = ode45(odefun, [0 10], [0; 0]);